# 本机验证记录

## 0.4.0 手机聚焦输入框实际内容同步

移除本机发送记录，新增认证后的 editor.snapshot，通过 Android InputConnection.getExtractedText 读取完整文字和 UTF-16 选区。Mac 输入页可见且有焦点时约每 400ms 刷新，连续输入期间定期读取；清除旧快照而不拼接本地输入。只支持应用明确提供的完整文本，2048 UTF-16 单位上限，不支持／超限时显示原因。密码框、锁屏在读取前拒绝；认证在接收端、IME 主线程和完成读取后复核。

`./scripts/test.sh` 全部通过：Swift 12、Kotlin 协议 12、Android IME 6、外观 2、跨语言 TLS/WebSocket 互通 1，合计 **33 项，零失败、零跳过**。覆盖未配对／撤销读取、密码框及锁屏零读取调用、文本变化／删除／新输入框／空文本／选区、部分文本／超长拒绝、客户端非法响应，以及真实 Swift 与 JVM 服务的已有中文快照和写入后读取。

Mac Release、Android Debug 构建及双方签名验证通过，Android lint 0 错误、8 警告。最后补充 Mac 输入视图可见性门限后重新完成 Release 构建。UI 检查因 Mac 锁屏无法继续；没有自动替换运行中的旧应用，没有完成小米 13 实机快照验收。解压目录里的旧运行副本保留，避免在运行中覆盖；新版 zip/APK 均为 0.4.0，更新需两端一起安装。

## macOS 0.3.8 点击输入恢复焦点

现场界面显示已连接、PAUSED、输入框为第一响应者，确认问题为暂停状态而非完全失焦。此前有光标却未自动恢复，点击记录也未聚焦输入框。现在明确点击输入框或记录区会恢复并定位到输入框；显示暂停占位提示，暂停后的确认回调也不会把状态误写成已就绪。跨窗口仍暂停，拒绝保护不变。

Release 构建、签名及 11 项现有 Swift 测试通过。已替换并启动 0.3.8，检查了未连接提示。尝试恢复原有手机连接失败，尚未完成新版实机点击恢复和输入验证。

## macOS 0.3.7 可见输入记录

实时输入增加工作台与小窗共享的只读记录。手机确认 ok 后追加实际发送事件，拒绝、丢弃及未知投递不计入已确认记录；退格／方向键等显示为操作标记，避免假装镜像手机编辑器。记录仅在内存中，最多 4096 UTF-16 单位并保留完整组合字符，可手动清空，断开或密码框拒绝时自动清空。

Mac Release 构建和签名检查通过；Swift 11 项测试通过，新增 Unicode 边界、记录清空与操作标记验证，顺序发送测试同时核对确认事件；已有拒绝／未知投递测试确保不调用确认回调。实际打开工作台及小窗核对记录、清空和输入区域布局；没有向实机发送测试文字。最新 Mac 包为 0.3.7，Android 无变化。

## macOS 0.3.6 页面导航及运行图标

扫码与连接设置改为工作台内页面，侧栏高亮当前页；离开扫码页停止摄像头，并拒绝已离开页面的延迟扫码回调。小窗右上角改为展开工作台按钮，暂停／继续放在底部。启动时从包内 ICNS 设置 applicationIconImage，明确刷新运行中应用图标，补充 CFBundleVersion。

Release 构建、签名检查、10 项 Swift 测试通过（图标赋值前运行，图标赋值后重新构建）。检查到同时运行的 0.3.4 解压副本与构建副本，已通过界面退出两者、更新解压副本并启动最新版。实际观察到扫码页位于主窗口且侧栏选中；后续 UI 操作遇到用户正在操作而停止。⌘Tab 图标显示仍待用户目视确认。

## macOS 0.3.5 B 工作台视觉对齐与小窗精简

重新对照原 B/C HTML 预览：工作台采用细边框输入面板、底部状态条、自定义模式分段及统一按键样式；扫码配对和连接设置改为侧栏整行导航。保留系统明暗模式，删除宣传文案。状态栏弹窗精简为 360×270，只呈现连接、输入和状态，次要操作放入更多菜单；整段模式仍可发送。

Mac Release 构建、签名检查、git diff --check 通过。实际启动并查看浅色工作台、小窗和更多菜单，验证菜单返回工作台。此次为视觉修改，未重新运行协议测试；没有重新验证双屏位置，也未修改 Android。产物 `artifacts/WiFiRemoteInput-0.3.5-macOS.zip`。

## macOS 0.3.4 双屏焦点后续修正

用户反馈 0.3.3 在 A 屏弹出后立即消失、B 屏闪动，说明上一版未解决问题。移除弹窗路径中的 NSApp.activate，仅在明确打开工作台时激活应用。实时输入仍要求当前输入视图是键窗口的第一响应者；移除应用整体 active 状态的额外限制，以兼容状态栏弹窗独立获取键盘焦点。

Release 构建、签名验证及 10 项已有 Swift 测试通过。UI 自动检查本次超时，不能据此确认双屏问题已解决；需退出旧进程后打开 0.3.4，在两屏分别检查弹窗停留、输入焦点及点击外部关闭。Android 不变。

## macOS 0.3.3 多显示屏弹窗定位

针对 A 屏点击却在 B 屏弹出的反馈，改为使用点击回调的状态栏按钮作为锚点，在激活应用前显示弹窗并设置键窗口，每次打开新建弹窗避免沿用旧屏幕窗口。之前的激活顺序可能使工作台所在屏幕影响状态栏定位，这是待实机复测的原因判断。

Mac Release 构建、签名校验及 10 项现有 Swift 测试通过；实际启动确认工作台入口仍能打开弹窗。这些测试不覆盖双屏菜单栏的位置；从 A、B 两屏分别点击图标仍需用户复测。安装包 `artifacts/WiFiRemoteInput-0.3.3-macOS.zip`；Android 保持 0.3.2。

## 0.3.2 两端应用图标统一

Mac 应用包内嵌 AppIcon.icns，覆盖 Finder／Dock 所用图标；Android 启动器图标改为同款键盘与手机图形，适配系统圆形／圆角遮罩及 Android 13 主题图标，接收服务通知也使用同源单色图标。

实际执行 Mac Release 构建、Android assembleDebug / lintDebug、双端签名校验，并核对 APK 图标资源入口和 Mac CFBundleIconFile。产物为 `artifacts/WiFiRemoteInput-0.3.2-macOS.zip` 与 `artifacts/WiFiRemoteInput-0.3.2-debug.apk`。尚未在手机启动器实机查看新版图标。

## macOS 0.3.1 状态栏弹窗修正

将原先缩小的独立窗口替换为 NSStatusItem + NSPopover：点击顶部图标打开瞬时弹窗，收起时暂停输入；可回到工作台，关闭工作台后保留状态栏入口。两处共享 Client，心跳集中到应用级，不随弹窗关闭停止。新增同源绘制的单色状态栏图标和薄荷绿应用图标，构建时生成全部 ICNS 尺寸。

实际启动验证弹窗锚定顶部、弹窗内编辑、返回工作台；macOS 10 项测试通过，Release 构建和签名验证通过。Android 未修改，继续使用 0.3.0 APK。此次没有连接手机发送测试输入。

## 0.3.0 界面与实时输入

两端采用 B 工作台方向，删除宣传文案，跟随系统浅色／深色模式；Mac 新增可切换的紧凑小窗。实时输入仅发送 macOS 输入法确认后的 Unicode；保留整段发送。窗口失焦暂停，密码框／锁屏／编辑器拒绝清空待发送队列，网络失败不重发。

实际执行 `./scripts/test.sh`：Swift 10 项、Kotlin 协议 11 项、Android IME 3 项、Android 浅色／深色启动 2 项、Swift ↔ Kotlin 实际 TLS/WebSocket 互通 1 项，合计 **27 项通过，零失败、零跳过**。新增测试验证拼音组合期间不发送、确认中文只发送一次、取消组合无残留、按键顺序、文字批量合并、队列暂停清除、拒绝／未知投递不重试。

Mac Release、Android APK 构建及双方签名验证成功。Android lint 为 0 错误、6 警告。实际启动 Mac 并检查浅色工作台与紧凑小窗，布局和切换正常。Android 两种主题经过 Robolectric Activity 启动验证，尚未检查新版 HyperOS 实机外观；macOS 实际中文输入法候选窗口及手机实时输入需用户安装新版后验收。先前 0.2.0 的实机反馈不作为新版实时输入的实机验证。

产物：`artifacts/WiFiRemoteInput-0.3.0-debug.apk`、`artifacts/WiFiRemoteInput-0.3.0-macOS.zip`，校验和 `artifacts/SHA256SUMS-0.3.0`。更新安装保留原配对；无需重新启用输入法。

## 0.2.0 扫码配对更新

用户已反馈 0.1.0 基础功能在实机跑通。本次新增手机二维码及 Mac 摄像头扫码自动配对，两端版本均为 0.2.0。Android 更新安装保留原证书和配对记录。

本次实际执行：Swift 测试 4 项、Kotlin 协议测试 11 项、IME 测试 3 项、跨语言互通测试 1 项，合计 **19 项全部通过，零跳过**。互通测试使用 Android 生产二维码生成代码输出图片，经过 macOS Vision 解码，自动执行固定证书 TLS 配对，并验证二维码不可重复使用、重连认证、中文和按键。另覆盖错误格式、未知版本、外网地址、缺失指纹及二维码过期/撤销状态。

Android APK、Mac Release 应用构建成功。摄像头权限说明已写入 Mac 应用；摄像头仅在打开扫码窗口时启动，关闭或识别成功后停止。图片解码至网络配对链路已在本机自动验证；用户随后反馈扫码配对「已验证 ok」，确认实机扫码流程通过。基础输入与扫码配对均有用户实机反馈，未逐项反馈的其他边界仍以自动测试结果为准。

新版产物：`artifacts/WiFiRemoteInput-0.2.0-debug.apk`、`artifacts/WiFiRemoteInput-0.2.0-macOS.zip`。手机打开「显示配对二维码」，Mac 点击「扫码配对」并允许摄像头访问即可；无摄像头时仍可手动配对。

以下保留首次 MVP 的验证记录。


首次验证日期：2026-09-18；开发基于起始提交 `5d5b917`。

## 环境与产物

- Apple Silicon，macOS 15.7.4；Xcode 26.3；Swift 6.2.4。
- 自动安装 Homebrew OpenJDK 17.0.20.1、Android command-line tools、Android SDK 35 / Build Tools 35.0.0。
- Gradle Wrapper 8.11.1 下载及 SHA-256 校验已实际执行；依赖版本保存在 `android/app/gradle.lockfile`。
- APK：`android/app/build/outputs/apk/debug/app-debug.apk`。APK 签名验证通过；包名 `dev.wifiremote`，版本 0.1.0，minSdk 29，targetSdk 35。
- Mac：`macos/build/WiFi Remote Input.app`。Release 构建及本地临时签名验证通过，实际启动窗口并检查了未连接状态下禁止发送、空地址拒绝连接。
- 便于传输的副本：`artifacts/WiFiRemoteInput-0.1.0-debug.apk` 和 `artifacts/WiFiRemoteInput-macOS.zip`，附 `artifacts/SHA256SUMS`。这些构建输出不会纳入 Git。

## 实际执行的验证

`./scripts/test.sh` 成功：

| 测试 | 数量 | 结果 |
| --- | ---: | --- |
| Kotlin 协议、安全边界 | 10 | 全部通过 |
| Android IME / InputConnection（Robolectric，API 35） | 3 | 全部通过 |
| Swift ↔ Kotlin TLS 1.3 / WebSocket 互通 | 1 | 通过 |
| Swift 地址与证书指纹校验 | 2 | 全部通过 |

合计 **16 项，零失败，零跳过**。另执行 Android `assembleDebug`、`lintDebug` 及 macOS Release 构建。

互通测试运行真实 Swift URLSession 客户端和 Android 使用的同一份 Kotlin 接收服务代码，验证错误证书拒绝、未配对输入拒绝、首次配对、设备密钥重连认证、中文 `你好，小米 13 👋` 和全部六种按键。服务端在 JVM 上运行，测试设备密钥只存于临时目录并在测试后清理。

IME 测试执行真正的 `RemoteIme` 逻辑和 Android `BaseInputConnection`，核对中文缓冲区、编辑器动作、按键对、输入结束后拒绝，以及普通密码、可见密码、网页密码和数字密码框同时拒绝文本与按键。

这两组测试分别验证网络链路和 Android 编辑器链路；**没有安装或控制实物小米 13，也没有声称已在 HyperOS 实机输入框完成验收**。没有运行 ADB 或使用 USB 调试。

## TLS 兼容性取舍

TLS 1.2-only 配置在本机 URLSession/JVM 的连续断开、重连测试中出现超时；最终版本明确只启用 TLS 1.3，不保留该降级路径。最终完整测试针对这一配置通过。Android 最低版本因此设为 Android 10；[Android 官方说明](https://developer.android.com/about/versions/10/behavior-changes-all)确认 Android 10 开始内置并默认启用 TLS 1.3。小米 13 的实际 Android TLS 实现仍属于下面的实机验收范围。

Lint 没有错误。非阻断警告包括当前中文界面的资源化建议，以及 Bouncy Castle 依赖中未使用的信任管理器类。生产 Android 代码仅用 Bouncy Castle 生成本地证书，不使用该信任管理器建立客户端连接；Mac 对证书做完整 SHA-256 固定校验，并已通过错误证书拒绝测试。备份和设备迁移均显式禁用。

## 首次交付时的小米 13 验收清单（后续反馈见上文）

按根目录 README 的安装与配对步骤操作：

- 安装 APK，启用并选择 WiFi Remote Input，启动接收服务。
- Mac 填写手机地址及手机显示的完整证书指纹，然后输入两分钟有效的配对码。
- 在手机应用内普通测试框以及备忘录正文分别发送 `你好，小米 13 👋`，核对显示。
- 验证 Enter、Backspace、四个方向键；测试密码框，确认内容不变且 Mac 显示拒绝。
- 断开后留空配对码重连；撤销配对后确认旧 Mac 无法再输入；停止服务后确认连接失效。
- 在 HyperOS 切换到其他应用后确认前台接收服务仍工作；如系统限制后台运行，按 README 调整该应用的电池设置。

## 0.5.0 设备名称与多手机验证（2026-09-19）

- Swift 19 项、Android/JVM 24 项测试全部通过，共 43 项；没有跳过双端互通测试。Android lint 与 APK 构建通过，Mac release 构建及两端签名验证通过。
- 客户端测试直接运行 `Client` / `DeviceSession`，覆盖切换丢弃旧队列、独立连接、显式群发和逐设备按键顺序、未连接目标阻止群发、密码拒绝暂停整组且不重试、名称持久化、证书去重、忘记设备隔离。
- Android 测试验证名称只在成功认证后保存、重启后还原、撤销清除、旧 token hash 兼容迁移、错误凭据不能改名、非法名称不能消耗配对码。
- Swift 客户端对两个独立的生产 Kotlin TLS/WebSocket 接收端同时建立连接，验证双向名称交换、A 的密钥无法认证 B、只向 B 输入、并行发送 Unicode 到 A 和 B。此测试使用本机 JVM 接收端，不代表两台实机验收。
- 实际启动 0.5.0 Mac App，检查深色工作台设备列表、同步勾选、名称设置及状态栏小窗；旧单设备记录正确迁移，重启默认关闭同步输入。已替换 artifacts 中旧 App 并启动新版。
- Android 0.5.0 APK 已打包。手机覆盖安装后重连才能补全名称；两台 Android 实机切换／同步输入、HyperOS 后台行为仍需在手机上验收，未使用 ADB 或 USB 调试。

## 0.6.0 单框同步、Enter 与焦点验证（2026-09-20）

- Swift 25 项、Android/JVM 27 项，共 52 项测试通过；Android lint、APK 构建及 Mac release 构建通过。
- 新增原生 NSTextView 测试：同一框保留已确认中文与 emoji、候选阶段不发送且不被快照覆盖、选区替换与删除连续哈希、手机回传不回送、失焦丢弃候选词、重新获得原生焦点触发恢复、Enter 和显式换行分离。客户端回归覆盖请求在途时失焦再恢复。
- Android IME 测试覆盖镜像编辑、文本／光标／输入框变化拒绝过时编辑、密码框在读取前拒绝、显式发送、NO_ENTER_ACTION 与 SEND 组合、自定义 actionId、换行及虚拟键标记。协议测试覆盖认证和编辑字段边界。
- 实际 Swift ↔ Kotlin TLS/WebSocket 测试验证新编辑报文、两种语言的内容哈希一致及重复旧编辑拒绝；既有双接收端、密钥隔离、Unicode、QR 和快照测试继续通过。
- 已实际启动 Mac 单框界面并确认只存在一个实时编辑控件。微信是否真正发送仍需安装新版 Android 后实机验证；接口返回 ok 不代表聊天消息已送达。未使用辅助功能模拟点击、ADB 或 USB 调试。

## 0.6.1 Mac 中文输入隔离与暂停提示（2026-09-20）

- 删除暂停刷新／失焦路径中的 `inputContext.discardMarkedText()` 及无条件合成 marked text 操作；仅在本控件确有组合文字时，通过父类本地插入清理对应范围，不转发到手机。无组合文字时清理幂等，不触碰系统输入上下文。
- 输入区在手机返回 no_editor 时显示原因与操作指引，保留到手机实际返回有效快照或断开连接；有效快照到达后提示可以继续。
- 27 项 Mac 测试通过，包括另一原生编辑器正在选词时连续 100 次后台暂停刷新不改变其组合文字，以及 no_editor 提示保留和断开清理。Mac release 构建通过。Android 未改动，继续使用 0.6.0。跨应用实际中文输入体验仍需用户复验。

## 0.6.2 友好设备名称（2026-09-20）

Mac 使用 SCDynamicStoreCopyComputerName；若为大写字母数字资产编号，回退到本机 system_profiler 的 machine_name（只保留该字段，不记录硬件报告）。Android 使用 Settings.Global.DEVICE_NAME，系统名称缺失时采用可读 Build.MODEL 或品牌名称。手动名称优先，支持恢复自动命名；名称在重新认证后同步。新增命名回退、字符清理及 emoji 长度测试。本机系统名为编号，硬件名为 MacBook Pro。手机返回的实际系统名称仍需安装新版 Android 后验证。

## 0.6.3 回车模式样式（2026-09-20）

将原生菜单替换为自定义分段按钮，与界面薄荷绿选中态、圆角和背景一致，支持系统浅深色。Mac release 构建及签名检查通过；实际检查深色工作台、小窗布局与模式切换，并恢复用户原先的发送模式。仅修改 UI，无协议或 Android 改动。

## 0.6.4 扫码配对与列表删除（2026-09-20）

移除 Mac 连接设置页面和相关导航，扫码错误提示不再建议手动配对。手机列表只显示名称、连接状态，并提供逐项忘记按钮。删除非当前设备不会切换当前手机或断开其他连接，删除最后一台返回未配对状态；删除会暂停输入并从同步接收者中移除该设备。29 项 Swift 测试通过，含删除对象隔离和记录持久化测试；Android 未改动，继续使用 0.6.2。

## 0.6.5 工作台精简（2026-09-20）

移除回车、退格和四个方向键的屏幕按钮；底部保护提示与断开操作合并为一行。实体键盘处理不变。Mac release 构建和签名检查通过，Android 无改动。


## Android 0.6.6：B 方案首页（2026-09-20）

首页改为键盘环形视觉、连接状态、电脑 → 手机名称、输入法就绪状态和当前主要操作；浅色、深色跟随系统。使用自定义圆角按钮背景和点击反馈。移除技术信息和测试输入框；二维码内嵌展示，收起或页面销毁即撤销临时码，过期／使用后清除；已配对电脑可以单独忘记，手机名称可编辑或恢复自动命名。

Android 30 项测试全部通过（含 JVM ↔ Swift 实际互通，无跳过），lint 0 错误、5 警告；APK 构建和签名验证通过。新增输入法已选中时不重复显示设置入口的回归检查，更新日夜模式检查；Robolectric 原生绘制的浅色／深色首页预览位于 artifacts/previews，已检查排版。此预览不是小米 13 截图。用户随后反馈「验收成功」，确认本次 Android 界面更新实机验收通过；未逐项反馈的边界仍以自动测试结果为准。Mac 本次未改动，继续使用 0.6.5。


## macOS 0.6.7：全局快捷键（2026-09-20）

新增系统快捷键注册，默认 ⌥⌘I 呼出小窗并聚焦输入框。侧栏支持录入、停用及持久保存；冲突时保留旧绑定。仅注册指定按键，不安装全局键盘监听、不申请辅助功能或输入监控权限。呼出时先隐藏工作台，等待应用激活完成再展示小窗，避免焦点切换关闭弹窗。

Swift 31 项测试通过，新增注册冲突／原绑定保留、禁用与保存、普通输入及 Option 单键组合拒绝检查。Release 构建和签名验证通过。实际 UI 验证自定义录入与恢复默认，并通过应用菜单调用同一呼出动作，确认小窗显示且实时输入框成为焦点。自动化合成快捷键不能可靠证明系统物理按键路径，实际全局热键以及双显示屏定位仍需用户键盘验收。本次未连接手机发送文字；Android 保持 0.6.6。


## macOS 0.6.8：Esc 收起小窗（2026-09-20）

Esc 在小窗内暂停输入并关闭弹窗，未连接或已暂停时同样生效。只处理当前小窗的本地按键事件；编辑器有中文组合文本时交还输入法处理，不影响其他窗口或应用。关闭时移除事件监听。Release 构建及签名校验通过，UI 实际呼出并聚焦小窗后按 Esc，确认弹窗消失。IP 变化仍需重新扫码；局域网自动发现尚未实现。
